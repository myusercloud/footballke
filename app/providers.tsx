// Superseded by components/analytics/AnalyticsProvider.tsx (Phase 5).
// All analytics now route through lib/analytics/ — PostHog is never
// imported directly outside of lib/analytics/posthog.provider.ts.
export { AnalyticsProvider } from "@/components/analytics/AnalyticsProvider";
